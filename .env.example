# Local Vite development:
VITE_API_BASE_URL=http://localhost:8001
# Production Docker builds can leave this blank to use the same-origin /api proxy.

# Docker preview / VPS local bind:
WEB_BIND=127.0.0.1
WEB_PORT=8080
