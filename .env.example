# Frontend build-time API target.
# Leave empty in production so the calculator calls same-origin /api/* through nginx.
VITE_API_BASE_URL=

# API and bot secrets live only in private/runtime environment files:
# - /srv/solane-run/shared/solane-run.env
# - /srv/solane-run/shared/solane-bot.env
